alert( null > 0 );  // false
alert( null >= 0 ); // true
alert(null < 0);    // false
alert( null <= 0 ); // true
alert( null == 0 ); // false
alert( null != 0 );    // true

//При нестрогом сравнении null преобразуется в 0, поэтому 2 и 4 сравнения true.